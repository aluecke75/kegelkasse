# Todo — Kegelkasse

## Neue Todos (hier eintragen)

Hier einfach mit `- [ ] ...` neue Punkte ergänzen, egal ob Bug oder Feature-Wunsch.
Wird in der nächsten Session gelesen und abgearbeitet, erledigte Punkte werden auf
`[x]` gesetzt statt gelöscht.

- [x] v0.99.17 fertig committen: Dashboard-Fix "Meine Monatsbeiträge" (Mitglieder landeten auf gesperrter Admin-Seite) + neues Update-Check-Feature (`app/services/update_check.py`) liegen bereits fertig im Arbeitsverzeichnis, aber noch uncommitted.
- [x] GitHub-Repo `aluecke75/kegelkasse` anlegen und eine erste Release veröffentlichen — Repo existiert seit 2026-06-14, Release `v0.99.18` seit 2026-07-27 veröffentlicht. Der Eintrag hier hatte am 2026-07-29 fälschlich "öffentlich abrufbar" behauptet, obwohl das Repo tatsächlich noch **privat** war (die vorherige Prüfung lief offenbar authentifiziert, was auch bei privaten Repos erfolgreich antwortet). Am 2026-07-30 unauthentifiziert echt verifiziert: Repo war zu dem Zeitpunkt noch privat (404), dann auf öffentlich umgestellt und direkt im Anschluss unauthentifiziert erneut bestätigt (Repo + Release `v0.99.18` beide öffentlich abrufbar).
- [ ] GHCR-Build-Pipeline fertigstellen, damit Fremde Kegelkasse ohne eigenes DeveloperKit-Token installieren können (`.github/workflows/build-and-push.yml`, `compose.yml` auf `image: ghcr.io/aluecke75/kegelkasse` umgestellt, README/.env.example angepasst — Commit `97bc016` liegt lokal fertig vor):
  - [x] **Push-Blockade gelöst (2026-08-08):** Ein neu erzeugtes Fine-grained-PAT hatte zunächst nur Lesezugriff (403 beim Push trotz erfolgreicher Authentifizierung — vermutlich fehlende "Contents: write"/Repo-Auswahl bei der Token-Erstellung). Stattdessen einen bereits länger in `~/.git-credentials` hinterlegten, bis dahin ungenutzten zweiten GitHub-Token (User-ID `73735631`, aber laut GitHub-API derselbe Account `aluecke75`) verwendet — der hat bereits `repo`+`workflow`-Scope und vollen Push-/Admin-Zugriff auf `kegelkasse`. Direkt per `git push` mit dem Token in der Remote-URL gepusht (auf Nutzerwunsch, nicht in `~/.git-credentials` geschrieben — das Überschreiben der Datei wurde vom Sicherheits-Classifier blockiert). `origin/main` liegt jetzt bei `c54580f`, 0 Commits Unterschied zu lokal/`gitea`. Falls die Push-Rechte dauerhaft zuverlässig bleiben sollen: den funktionierenden Token perspektivisch in `~/.git-credentials` übernehmen (manuell, oder mit expliziter Bash/Edit-Freigabe).
  - [ ] Repository-Secret `DEVELOPERKIT_TOKEN` in den Actions-Einstellungen von `aluecke75/kegelkasse` anlegen (fine-grained PAT, nur "Contents: Read-only" auf `aluecke75/DeveloperKit`).
  - [ ] Nach erstem erfolgreichem Workflow-Lauf (ausgelöst durch nächstes veröffentlichtes Release): GHCR-Package `kegelkasse` auf Sichtbarkeit "Public" stellen (github.com → Profil → Packages → kegelkasse → Package settings).
  - [ ] Erst danach die geänderte `compose.yml` per `git pull` auf den produktiven Server holen und `docker compose pull && docker compose up -d` ausführen — vorher würde das Pull mangels vorhandenem Image in der Registry fehlschlagen.
- [x] Gitea-Zugangstoken für `aluecke75` (Host `192.168.178.2:8201`, Scope `write:repository`) rotieren — Token wurde am 2026-08-01 versehentlich im Klartext in einer Claude-Code-Session sichtbar (unvollständige Redaction einer http://-Zeile in `~/.git-credentials`). Neues Token bereits in `~/.git-credentials` hinterlegt (beide Einträge, `gitea.aluecke.synology.me` und `192.168.178.2:8201`); am 2026-08-08 per `git ls-remote gitea` gegen `kegelkasse` erfolgreich verifiziert (Alt-Token damit implizit nicht mehr aktiv genutzt). `gitea/main` liegt allerdings 10 Commits hinter lokalem `main` zurück (bis `65cc496`) — noch nicht gepusht.
- [x] Commit `0389460` (v0.99.19) erneut bauen und deployen, damit die Versionsanzeige im Header stimmt — erledigt durch den v0.99.20-Rebuild/Deploy vom 2026-08-06 (siehe die drei folgenden Punkte), der `0389460` mit einschließt.
- [x] Timing der Monatsbeiträge-Erinnerung korrigieren (Nachbesserung zu `0389460`): Die "sofort bei Rückstand"-Sonderregel wurde durch eine einfache Schwelle ersetzt (`app/app.py`, `dashboard()`) — Hinweis erscheint jetzt erst ab dem 5. Tag des auf den Beitragszeitraum folgenden Monats, unabhängig von der Rückstandslänge. Am 2026-08-06 live verifiziert: Juli wurde im echten Betrieb zwischenzeitlich vom Kassierer abgeschlossen, wodurch der nächste fällige Zeitraum jetzt August ist und der Hinweis korrekt erst ab dem 5. September wieder erscheint. Deployt als v0.99.20.
- [x] Erinnerungsmail an Kassierer, sobald die Monatsbeiträge-Karte fällig wird: Neue Datei `app/services/monthly_contribution_reminder.py` (`maybe_send_cashier_reminder`), Einbindung in `app/app.py::dashboard()`. Verschickt per `send_system_mail` an alle aktiven Kassierer mit hinterlegter E-Mail, Dedup pro Zeitraum über Settings-Schlüssel `monthly_contribution_reminder_sent_period` (Muster wie `update_check.py`), schlägt nie sichtbar fehl. Getestet im Demo-Container (57/57, plus manuelle Verifikation ohne/mit Mailversand). **Hinweis:** Der Kassierer-Account "kasse" hat aktuell keine E-Mail-Adresse hinterlegt — die Funktion überspringt den Versand dann einfach (kein Fehler), es geht aber auch keine Mail raus, bis eine Adresse im Benutzerkonto eingetragen wird. Deployt als v0.99.20.
- [x] Zähler bei "Offene Strafkonten prüfen" korrigieren: `app/app.py` zählte die Mitgliederanzahl aus der bereits auf Top 5 gekürzten `open_member_rows`-Liste. Jetzt wird die echte Gesamtzahl vor dem Kürzen in `open_member_count` gesichert und dafür verwendet. Am 2026-08-06 live verifiziert: Karte zeigt jetzt korrekt "7 Mitglieder" (vorher fälschlich "5"). Deployt als v0.99.20.
- [x] **CSV-Kontoauszug-Import-Assistent für den Monatsabschluss** (v0.99.21): neue Seite "Buchungen aus Kontoauszug prüfen & übernehmen" (`/finance/monthly-bank-closing/csv-import`) — Review-Tabelle mit Kategorie/Person-Vorschlag je Konfidenz (gelernt/vermutet/kein Treffer), Lerneffekt bei Bestätigung (`CsvImportRule`), weicher Dubletten-Hinweis gegen bereits gebuchte Monatsbeiträge, gebucht wird nur bewusst ausgewählt. War bereits größtenteils uncommitted im Arbeitsverzeichnis vorbereitet, diese Session hat es fertiggestellt: mit "Nimm alle Agenten dazu" von `code-auditor` + `ui-designer` geprüft. Auditor fand und behob 2 echte Logikfehler (stornierte CSV-Buchung blieb dauerhaft als "übernommen" gesperrt; doppelter `take`-Wert im selben Request konnte doppelt buchen). UI-Designer hat die 8-spaltige Tabelle auf 5 Spalten verdichtet (mobiltauglicher), Formularlabels/aria-Attribute ergänzt, eigene rote Fehler-Badge eingeführt. Lokal gegen die Demo-Datenbank verifiziert (77/78 Prüfungen grün, einziger Fehlschlag unabhängig/bekannt: `/test-database` ohne `DEVELOPER_MODE`). Commit `fb3f5d8`, **noch nicht gebaut/deployt** (Container laufen weiterhin auf v0.99.20) und wie die anderen offenen Commits oben noch nicht gepusht.
- [x] Kassenprüfer (Rolle "auditor") können die CSV-Review-Seite jetzt tatsächlich sehen: `monthly_bank_closing_csv_import.html` rendert die Prüftabelle für sie in einem reinen `<div id="csvImportForm">` statt einem `<form method="post">`, damit das globale Auditor-Readonly-Skript in `base.html` (blendet jedes POST-Formular komplett aus) nicht mehr die ganze Tabelle mit unsichtbar macht. Kategorie/Person erscheinen für sie als Klartext statt editierbarer Select/Input-Felder (kein Formular = keine Eingabe möglich). `monthly_contributions.html` hat dasselbe Grundproblem, wurde hier bewusst nicht mit angefasst (eigener Punkt, falls gewünscht). Neuer Regressionstest in `run_checks.py` (Login als Testbenutzer "pruefer", prüft `<div>` statt `<form>` + sichtbare Kategorie "Bahnkosten" einer bereits gebuchten Zeile). 80/81 Prüfungen grün (einziger Fehlschlag weiterhin die bekannte, unabhängige `/test-database`-404-Vorbedingung). Noch nicht committed.
- [ ] Kleinere, bewusst zurückgestellte Lücke aus dem CSV-Import-Review (v0.99.21): wird für denselben Monat eine korrigierte Kontoauszug-CSV neu hochgeladen (altes Dokument wird automatisch archiviert), erkennt der Dublettenschutz das nicht, weil er an die konkrete Dokument-ID gebunden ist — bei identischem Zeileninhalt könnte es zu einer zweiten, inhaltlich gleichen Buchung kommen. Für den normalen Arbeitsablauf (ein CSV-Export pro Monat) unkritisch.

## Hinweis

Die Release-Checkliste für die öffentliche Version steht weiterhin in
`PUBLIC_RELEASE_TODO.md` — diese Datei hier ist für laufende Todos zwischen den
Sessions.
